/// <summary>
/// Keys used for identifying values in the Blackboard.
/// Using an enum prevents typos and improves maintainability.
/// </summary>
public enum BlackboardKey
{
    Score,
    SpeedMultiplier,
    Coin,
    PlayerLane
}
